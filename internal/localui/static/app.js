const content=document.querySelector('#content'),title=document.querySelector('#title');let report={},status={};
async function load(){try{[report,status]=await Promise.all([fetch('/api/v1/local/report').then(r=>r.json()),fetch('/api/v1/local/status').then(r=>r.json())]);document.querySelector('#activation').hidden=!(status.connectors||[]).some(item=>item.status==='SUCCEEDED'||item.status==='WARNING')||!(report.resource_count>0);render(active())}catch(e){content.innerHTML='<div class="panel">Local report could not be loaded.</div>'}}
function active(){return document.querySelector('nav .active').dataset.view}
function coverageMetric(){const coverage=report.coverage_percent||0,complete=report.coverage_evidence_completeness_percent||0,partial=(report.coverage_evidence_state||'UNKNOWN')!=='COMPLETE';return partial?`<div class="metric evidence"><span>Evidence completeness</span><strong>${complete}%</strong><small>Evaluable coverage: ${coverage}%</small></div>`:`<div class="metric"><span>Coverage</span><strong>${coverage}%</strong><small>Complete evidence</small></div>`}
function metrics(){return `<div class="metrics"><div class="metric"><span>Resources</span><strong>${report.resource_count||0}</strong></div><div class="metric"><span>Open findings</span><strong>${report.open_finding_count||0}</strong></div><div class="metric"><span>Critical</span><strong class="critical">${report.critical_count||0}</strong></div>${coverageMetric()}</div>`}
function value(v){if(v===null||v===undefined)return '';if(typeof v==='object')return v.name||v.id||'';return v}
function table(rows,cols){return `<div class="panel"><table><thead><tr>${cols.map(c=>`<th>${c[0]}</th>`).join('')}</tr></thead><tbody>${rows.map(r=>`<tr>${cols.map(c=>`<td>${value(r[c[1]])}</td>`).join('')}</tr>`).join('')}</tbody></table></div>`}
function render(v){title.textContent=v[0].toUpperCase()+v.slice(1);if(v==='overview')content.innerHTML=metrics()+table(report.priority_findings||[],[['Severity','severity'],['Finding','type'],['Resource','resource'],['Recommendation','recommendation']]);if(v==='findings')content.innerHTML=table(report.priority_findings||[],[['Severity','severity'],['Finding','type'],['Resource','resource'],['Risk','risk_score'],['Recommendation','recommendation']]);if(v==='coverage'){const unknown=report.coverage_unknown_signals||0;content.innerHTML=metrics()+`<div class="panel"><strong>Coverage evidence</strong><p>${report.coverage_evidence_state||'UNKNOWN'} · ${report.coverage_evidence_completeness_percent||0}% complete</p><p>Evaluable coverage: ${report.coverage_percent||0}%. ${report.coverage_missing_signals||0} missing signals, ${unknown} unknown.</p>${unknown?'<p class="next">Next: connect the evidence sources for unknown signal types before treating evaluable coverage as estate-wide coverage.</p>':''}</div>`}if(v==='connectors')content.innerHTML=table(status.connectors||[],[['Group','group'],['Connector','name'],['Status','status'],['Resources','resource_count'],['Error','error']])}
document.querySelectorAll('nav button').forEach(b=>b.onclick=()=>{document.querySelector('nav .active').classList.remove('active');b.classList.add('active');render(active())});document.querySelector('#refresh').onclick=load;load();
