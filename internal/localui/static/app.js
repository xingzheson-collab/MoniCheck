const content=document.querySelector('#content'),title=document.querySelector('#title');let report={},status={};
async function load(){try{[report,status]=await Promise.all([fetch('/api/v1/local/report').then(r=>r.json()),fetch('/api/v1/local/status').then(r=>r.json())]);render(active())}catch(e){content.innerHTML='<div class="panel">Local report could not be loaded.</div>'}}
function active(){return document.querySelector('nav .active').dataset.view}
function metrics(){return `<div class="metrics"><div class="metric"><span>Resources</span><strong>${report.resource_count||0}</strong></div><div class="metric"><span>Open findings</span><strong>${report.open_finding_count||0}</strong></div><div class="metric"><span>Critical</span><strong class="critical">${report.critical_count||0}</strong></div><div class="metric"><span>Coverage</span><strong>${report.coverage_percent||0}%</strong></div></div>`}
function table(rows,cols){return `<div class="panel"><table><thead><tr>${cols.map(c=>`<th>${c[0]}</th>`).join('')}</tr></thead><tbody>${rows.map(r=>`<tr>${cols.map(c=>`<td>${r[c[1]]??''}</td>`).join('')}</tr>`).join('')}</tbody></table></div>`}
function render(v){title.textContent=v[0].toUpperCase()+v.slice(1);if(v==='overview')content.innerHTML=metrics()+table(report.priority_findings||[],[['Severity','severity'],['Finding','type'],['Resource','resource'],['Recommendation','recommendation']]);if(v==='findings')content.innerHTML=table(report.priority_findings||[],[['Severity','severity'],['Finding','type'],['Resource','resource'],['Risk','risk_score'],['Recommendation','recommendation']]);if(v==='coverage')content.innerHTML=metrics()+`<div class="panel"><strong>Coverage evidence</strong><p>${report.coverage_evidence_state||'UNKNOWN'} · ${report.coverage_evidence_completeness_percent||0}% complete</p><p>${report.coverage_missing_signals||0} missing signals, ${report.coverage_unknown_signals||0} unknown.</p></div>`;if(v==='connectors')content.innerHTML=table(status.connectors||[],[['Connector','name'],['Status','status'],['Resources','resource_count'],['Error','error']])}
document.querySelectorAll('nav button').forEach(b=>b.onclick=()=>{document.querySelector('nav .active').classList.remove('active');b.classList.add('active');render(active())});document.querySelector('#refresh').onclick=load;load();
